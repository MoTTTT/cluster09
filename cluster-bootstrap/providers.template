terraform {
  required_providers {
    proxmox = {
      source = "bpg/proxmox"
      version = "0.77.1"
    }
  }
}

provider "proxmox" {
  endpoint = "PROXMOXURL"
  username = "PROXMOXUSER"
  password = "PROXMOXPASS"
  insecure = true
  tmp_dir  = "/var/tmp"
  ssh {
    agent = true
  }
}

