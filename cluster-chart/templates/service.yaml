apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-chart.fullname" . }}
  labels:
    {{- include "cluster-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-chart.selectorLabels" . | nindent 4 }}
