apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gitopsprod
resources:
  - gitopsprod.yaml
  - proxmox-secret.yaml
configMapGenerator:
  - name: gitopsprod-values
    files:
      - values.yaml=gitopsprod-values.yaml
configurations:
  - kustomizeconfig.yaml
