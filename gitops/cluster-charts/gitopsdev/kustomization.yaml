apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gitopsdev
resources:
  - gitopsdev.yaml
  - proxmox-secret.yaml
configMapGenerator:
  - name: gitopsdev-values
    files:
      - values.yaml=gitopsdev-values.yaml
configurations:
  - kustomizeconfig.yaml
