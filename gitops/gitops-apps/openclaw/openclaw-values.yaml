expose:
  type: route
  tls:
    certSource: secret
    secret:
      secretName: openclaw-secret
  route:
    parentRefs:
      - name: gateway
        namespace: kube-system
        kind: Gateway
    hosts: 
      - openclaw.podzone.cloud

externalURL: https://openclaw.podzone.cloud
